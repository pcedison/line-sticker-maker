# 🚀 Line 貼圖全能工具箱 - 專案部署與開發環境指南
**Local Development & Deployment Guide**

本指南將引導您如何將線上的 React (Vite) 專案下載至本地電腦，進行後續的開發、安全設定與正式環境部署。

---

## 階段一：將專案從 StackBlitz 下載至本地

1. **下載壓縮檔**：
   在 StackBlitz 線上編輯器中，查看左側檔案總管 (`FILES`) 面板的最上方。點擊 **「雲朵加上向下箭頭 (Download Project)」** 的圖示。
2. **解壓縮專案**：
   瀏覽器會下載一個 `.zip` 壓縮檔。請將其解壓縮至您電腦中的工作目錄（例如：`D:\Workspace\Line-Sticker-Toolkit`）。

---

## 階段二：建立本地端開發環境 (Local Environment)

要讓專案在本地電腦上順利運行，您需要安裝 JavaScript 的伺服器環境（Node.js）。

### 1. 安裝必要工具
* **Node.js**：請前往[Node.js 官方網站](https://nodejs.org/)，下載並安裝標示為 **「LTS (長期維護版)」** 的版本。
* **程式碼編輯器**：推薦下載並使用免費的 [Visual Studio Code (VS Code)](https://code.visualstudio.com/)。

### 2. 安裝專案依賴套件 (Dependencies)
1. 使用 VS Code 打開您剛剛解壓縮的專案資料夾。
2. 在 VS Code 頂部選單選擇 **「終端機 (Terminal) -> 新增終端機 (New Terminal)」**。
3. 在終端機中輸入以下指令，並按下 Enter 鍵：
   ```bash
   npm install
   ```
   *(系統會自動讀取 `package.json`，並將 Tailwind CSS、Lucide 圖示等必要套件下載至 `node_modules` 資料夾中。)*

### 3. 啟動本地測試伺服器
套件安裝完成後，在終端機輸入以下指令啟動專案：
```bash
npm run dev
```
終端機會顯示一段網址（通常是 `http://localhost:5173/`）。按住 `Ctrl` (或 Mac 的 `Cmd`) 點擊該連結，即可在瀏覽器中看到您的專案成功運行！

---

## 階段三：🔒 資安設定 (隱藏 API 金鑰) - 必做！

目前在測試階段，Google Gemini 的 API Key 直接寫在 `App.jsx` 的程式碼中。
**若要將專案上線對外公開，絕對不能將金鑰寫死在程式碼中**，否則極易遭惡意人士盜用額度。請務必依循以下步驟使用「環境變數」來保護金鑰：

### 1. 建立環境變數檔案
在專案的 **最外層根目錄**（與 `package.json` 位於同一層），新增一個名為 **`.env`** 的檔案。

### 2. 寫入 API 金鑰
打開 `.env` 檔案，填入以下內容（等號後面請換成您真實的 API 金鑰，**請勿加上引號**）：
```env
VITE_GEMINI_API_KEY=AIzaSy...請將這裡換成您真實的金鑰...
```

### 3. 修改 `App.jsx` 以讀取環境變數
打開 `src/App.jsx` 檔案，尋找約第 8 行的原始設定：
```javascript
// ❌ 修改前 (有安全風險)
const apiKey = "AIzaSy...你的金鑰..."; 
```
將其替換為 Vite 專屬的環境變數讀取語法：
```javascript
// ✅ 修改後 (安全讀取)
const apiKey = import.meta.env.VITE_GEMINI_API_KEY;
```

*(修改完畢後，請重新啟動 `npm run dev` 以確保環境變數生效，並測試系統是否能正常運作。)*

---

## 階段四：正式上線與部署 (Production Deployment)

當您完成所有的測試與修改，準備將網站對外公開時，請依照以下步驟將專案編譯為「靜態網頁」。

### 1. 編譯專案 (Build)
在終端機中輸入以下指令：
```bash
npm run build
```
指令執行完畢後，您的專案目錄中會多出一個名為 **`dist/`** 的資料夾。這就是經過極致壓縮、最佳化後的最終網站成品檔（包含純 HTML、CSS、JS 與圖檔）。

### 2. 選擇部署方案
因為本專案為「純前端靜態網頁（無後端資料庫）」，您有以下多種輕量且免費的部署選擇：

#### 方案 A：使用現代化靜態代管平台 (推薦，免費且具備 SSL 憑證)
推薦使用 **Vercel**、**Netlify** 或 **Cloudflare Pages**。
1. 將您的專案上傳至 GitHub。
2. 登入上述平台並綁定您的 GitHub 專案。
3. **重要**：在平台的 `Environment Variables (環境變數)` 設定區塊，新增一筆 `VITE_GEMINI_API_KEY`，並填入您的金鑰。
4. 點擊 Deploy，平台會自動幫您執行 Build 並產出一串對外公開的 HTTPS 網址。

#### 方案 B：部署至自建 VPS 或虛擬主機
如果您有自己的 Linux 伺服器 (VPS) 或 cPanel 主機：
1. 將剛剛編譯出來的 **`dist/`** 資料夾內「所有的檔案」透過 FTP/SFTP 上傳至伺服器。
2. 將您的網頁伺服器（如 Nginx 或 Apache）的網站根目錄 (Root Directory) 指向這些檔案即可。

---
🎉 **恭喜您！完成上述步驟後，您的「Line 貼圖全能工具箱」就正式具備了企業級的開發流程與部署標準了！**